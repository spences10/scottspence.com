[phases.setup]
nixPkgs = ["nodejs_24", "pnpm_10", "sqlite", "gcc", "pkg-config"]
aptPkgs = ["curl", "wget", "libsqlite3-dev", "sqlite3"]

[phases.install]
cmds = ["pnpm i --frozen-lockfile --config.engine-strict=false"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"